{
  "name": "dread-error-reporter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker proxy for Dread error telemetry (ADR-0010)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.11",
    "vitest": "^4.1.8",
    "wrangler": "^4.96.0"
  }
}
